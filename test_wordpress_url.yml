- name: Buat ringkasan status tiap website
  set_fact:
    wp_status_messages: "{{ wp_status_messages | default([]) + [msg] }}"
  vars:
    status_code: "{{ result.results[item_index].status }}"
    site: "{{ wordpress_sites[item_index] }}"
    msg: >-
      {% if status_code == 200 %}
      ✅ Website '{{ site.name }}' aktif dan berjalan normal (Status: {{ status_code }}).
      {% elif status_code in [301, 302] %}
      🔄 Website '{{ site.name }}' melakukan redirect (Status: {{ status_code }}).
      {% elif status_code == 403 %}
      ⛔ Website '{{ site.name }}' akses ditolak (Forbidden, Status: {{ status_code }}).
      {% elif status_code == 404 %}
      ❌ Website '{{ site.name }}' halaman tidak ditemukan (Not Found, Status: {{ status_code }}).
      {% elif status_code >= 500 %}
      ⚠️ Website '{{ site.name }}' error server (Status: {{ status_code }}).
      {% else %}
      ⚠️ Website '{{ site.name }}' status HTTP tidak biasa: {{ status_code }}.
      {% endif %}
  loop: "{{ range(0, wordpress_sites|length) | list }}"
  loop_control:
    loop_var: item_index

- name: Tampilkan ringkasan status
  debug:
    msg: "{{ wp_status_messages | join('\n') }}"
